<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RobotoTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <!--for alert dialog-->
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <!--for light theme-->
    <style name="LightTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/dark1</item>
        <item name="colorPrimaryDark">@color/dark1</item>
        <item name="colorAccent">@color/dark1</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorTertiary">@color/lightGrey1</item>

        <item name="android:textColorPrimaryInverseDisableOnly">@color/lightBlue</item>

        <item name="imageButtonStyle">@style/ImageButtonStyleLight</item>
        <item name="colorControlNormal">@color/white</item>
        <item name="android:colorForeground">@color/lightGrey1</item>

        <item name="colorControlActivated">@color/dark1</item>
        <item name="colorSwitchThumbNormal">@color/black</item>


        <item name="android:textColorPrimaryDisableOnly">@color/bubble2Light</item>
        <item name="android:colorForegroundInverse">@color/bubble1Light</item>

        <!--for back arrow button-->
        <item name="android:homeAsUpIndicator">@drawable/ic_back_indicator</item>

    </style>

    <style name="ImageButtonStyleLight" parent="Widget.AppCompat.ImageButton">
        <item name="android:tint">@color/dark1</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="DarkTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="DarkTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="FullscreenActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/black_overlay</item>
    </style>

</resources>
